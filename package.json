{
  "name": "monkeypaw-workspace",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "node --test",
    "check": "npm test && npm pack --workspaces --dry-run"
  }
}
