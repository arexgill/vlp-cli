# Contributing

## Local setup

- Use Node.js 20 or newer.
- Run `npm install` once after cloning.
- Run `npm test` for the focused suite.
- Run `npm run check` before sending changes.

## Scope

Keep changes small and aligned with the current phase.
